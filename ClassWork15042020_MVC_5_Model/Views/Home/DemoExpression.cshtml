@model ClassWork15042020_MVC_5_Model.Models.Prod

@{
    ViewBag.Title = "DemoExpression";
    Layout = "~/Views/_BasicLayout.cshtml";
}

<h2>DemoExpression</h2>

<table>
    <thead>
        <tr><th>Property</th><th>Value</th></tr>
    </thead>
    <tbody>
        <tr><td>Name</td><td>@Model.Name</td></tr>
        <tr><td>Price</td><td>@Model.Price</td></tr>
        <tr>
            <td>In stock</td>
            <td>
            @*1 variant*@
            @*@switch ((int)@ViewBag.ProdCount)
            {
                case 0:
                    @:Out of stock
                    break;
                case 1:
                    <b>Little left (@ViewBag.ProdCount)</b>
                    break;
                default:
                    @ViewBag.ProductCount
                    break;
            }*@
            @*2 variant*@
                @if (ViewBag.ProdCount == 0)
                {
                    @:not
                }
                else if (ViewBag.ProdCount == 1)
                {
                    <b>Little left (@ViewBag.ProdCount)</b>
                }
                else
                {
                    @ViewBag.ProdCount
                }
            </td>
        </tr>
    </tbody>
</table>

<div data-discount="@ViewBag.ApplyDiscount" data-express="@ViewBag.ExpressShip"
     data-suplier="@ViewBag.Supplier">
        Container with date attributes.
</div>

Discount: <input type="checkbox" checked="@ViewBag.ApplyDiscount"/>
Fast Delivery: <input type="checkbox" checked="@ViewBag.ExpressShip"/>
Supplier: <input type="checkbox" checked="@ViewBag.Supplier"/>

