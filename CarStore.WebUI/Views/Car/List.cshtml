@using CarStore.Domain.Entities
@model IEnumerable<Car>

@{
    ViewBag.Title = "Cars";
}

@foreach (var p in @Model)
{
    <div>
        <h3>@p.Name</h3>
        <p>@p.Description</p>
        <h4>@p.Price.ToString("# dol")</h4>
    </div>
}